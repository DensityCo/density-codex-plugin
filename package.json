{
  "name": "density-codex-plugin",
  "version": "0.0.0",
  "description": "This repository is a Codex marketplace for the Density plugin.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test plugins/density/test/*.mjs",
    "update:density-runtime": "node plugins/density/scripts/update-managed-cli-runtime.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "semver": "^7.8.5"
  }
}
