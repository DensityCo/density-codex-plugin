interface:
  display_name: "Setup"
  short_description: "Connect Density and verify hosted data access."
  default_prompt: "Use $setup to verify my Density connection, sign-in, and hosted dataset access."
policy:
  allow_implicit_invocation: false
