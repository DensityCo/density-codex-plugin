interface:
  display_name: "Density"
  short_description: "Analyze workplace history with the hosted Density MCP."
  default_prompt: "Use $density to answer my workplace question with the hosted Density MCP. Read density://schema once, query authorized evidence, and render a chart when requested."
policy:
  allow_implicit_invocation: true
